"""
UI Components Module.

This directory is reserved for reusable Streamlit UI components, 
such as custom chat renderers, citation cards, and sidebar controls.
Currently, components are consolidated in `app.py` for simplicity.
"""